# Setup tools (link & install)
redirect_sctp = executable(
    'redirect-sctp',
    ['redirect-sctp.c'],
    include_directories: rawrtc_includes,
    dependencies: rawrtc_dependencies,
    link_with: lib_rawrtc,
    install: true
)
